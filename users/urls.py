app_name = "users"

urlpatterns = [
    # path("users/", ManageUsersView.as_view(), name="user_list"),
    # path("users/new/", UserCreateView.as_view(), name="user_new"),
    # path("users/<pk>/information/", UserUpdateView.as_view(), name="user_edit"),
    # path("users/bulk/delete", UserBulkDeleteView.as_view(), name="user_bulk_delete"),
    # path("users/<pk>/assign/group/", UserAssignGroupView.as_view(), name="user_assign_groups"),
    # path("users/<pk>/assign/permission/", UserAssignPermissionView.as_view(), name="user_assign_permissions"),
    # path("users/<pk>/change/password/", CustomPasswordChangeView.as_view(), name="user_change_password"),
    # path("users/<pk>/deactivate/", DeactivateAccountView.as_view(), name="account_deactivate_view"),

]


